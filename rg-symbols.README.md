# rg-symbols.txt

Empty file. The `nm` (or equivalent symbol-table) dump against the
Cursor-bundled `rg` binary produced no output — the ripgrep build strips
symbols before shipping, so there is nothing to record here.
